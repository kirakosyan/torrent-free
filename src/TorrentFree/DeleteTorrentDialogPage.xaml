<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TorrentFree"
             xmlns:helpers="clr-namespace:TorrentFree.Helpers"
             x:Class="TorrentFree.DeleteTorrentDialogPage"
             x:DataType="local:DeleteTorrentDialogViewModel"
             BackgroundColor="#80000000"
             Padding="24">

       <Grid RowDefinitions="*"
             ColumnDefinitions="*">
              <Frame Padding="20"
                     CornerRadius="12"
                     BackgroundColor="{AppThemeBinding Light=White, Dark=#2D2D30}"
                     HasShadow="True"
                     VerticalOptions="Center"
                     HorizontalOptions="Center"
                     MaximumWidthRequest="420">
                     <VerticalStackLayout Spacing="16">
                            <Label Text="{helpers:Translate RemoveTorrent}"
                                   FontSize="18"
                                   FontAttributes="Bold"/>
                            <Label Text="{helpers:Translate DeleteFilesOptional}"
                                   FontSize="12"
                                   TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                            <Label Text="{Binding TorrentName}"
                                   FontSize="14"
                                   FontAttributes="Bold"/>

                            <HorizontalStackLayout Spacing="8">
                                   <CheckBox IsChecked="{Binding DeleteTorrentFile}"
                                             SemanticProperties.Description="Delete torrent file"
                                             SemanticProperties.Hint="When checked, the source .torrent file is deleted from disk"/>
                                   <Label Text="{helpers:Translate DeleteTorrentFile}"
                                          VerticalOptions="Center"/>
                            </HorizontalStackLayout>

                            <HorizontalStackLayout Spacing="8">
                                   <CheckBox IsChecked="{Binding DeleteDownloadedFiles}"
                                             SemanticProperties.Description="Delete downloaded files"
                                             SemanticProperties.Hint="When checked, downloaded content files are permanently deleted"/>
                                   <Label Text="{helpers:Translate DeleteDownloadedFiles}"
                                          VerticalOptions="Center"/>
                            </HorizontalStackLayout>

                            <HorizontalStackLayout Spacing="12"
                                                   HorizontalOptions="End">
                                   <Button Text="{helpers:Translate Cancel}"
                                           SemanticProperties.Description="Cancel remove torrent"
                                           BackgroundColor="#BDBDBD"
                                           TextColor="White"
                                           Clicked="OnCancelClicked"/>
                                   <Button Text="{helpers:Translate RemoveButton}"
                                           SemanticProperties.Description="Confirm remove torrent"
                                           SemanticProperties.Hint="Removes the torrent and applies selected file deletion options"
                                           BackgroundColor="#E53935"
                                           TextColor="White"
                                           Clicked="OnDeleteClicked"/>
                            </HorizontalStackLayout>
                     </VerticalStackLayout>
              </Frame>
       </Grid>
</ContentPage>
