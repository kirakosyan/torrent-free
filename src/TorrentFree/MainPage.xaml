<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:TorrentFree.ViewModels"
             xmlns:models="clr-namespace:TorrentFree.Models"
             xmlns:converters="clr-namespace:TorrentFree.Converters"
             xmlns:controls="clr-namespace:TorrentFree.Controls"
             xmlns:helpers="clr-namespace:TorrentFree.Helpers"
             xmlns:xaml="clr-namespace:Microsoft.Maui.Controls.Xaml;assembly=Microsoft.Maui.Controls.Xaml"
             xmlns:maui="clr-namespace:Microsoft.Maui.Controls;assembly=Microsoft.Maui.Controls"
             x:Class="TorrentFree.MainPage"
             x:DataType="viewmodels:MainViewModel"
             BackgroundColor="{AppThemeBinding Light={StaticResource PageBackgroundColorLight}, Dark={StaticResource PageBackgroundColorDark}}"
             Title="{helpers:Translate DownloadsTitle}">

       <ContentPage.Resources>
              <ResourceDictionary>
                     <!-- Converters (defined locally to ensure availability) -->
                     <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
                     <converters:StringNotEmptyConverter x:Key="StringNotEmptyConverter"/>
                     <converters:ObjectNotNullConverter x:Key="ObjectNotNullConverter"/>
                     <converters:ProgressConverter x:Key="ProgressConverter"/>
                     <converters:StatusColorConverter x:Key="StatusColorConverter"/>
                     <converters:OddIndexConverter x:Key="OddIndexConverter"/>

                     <!-- Status Colors -->
                     <Color x:Key="StatusQueued">#FFA726</Color>
                     <Color x:Key="StatusDownloading">#42A5F5</Color>
                     <Color x:Key="StatusPaused">#BDBDBD</Color>
                     <Color x:Key="StatusCompleted">#66BB6A</Color>
                     <Color x:Key="StatusSeeding">#26A69A</Color>
                     <Color x:Key="StatusFailed">#EF5350</Color>
                     <Color x:Key="StatusStopped">#9E9E9E</Color>

                     <!-- Card Style -->
                     <Color x:Key="CardBackground">#FFFFFF</Color>
                     <Color x:Key="CardBackgroundDark">#1F1F23</Color>
                     <Color x:Key="CardBackgroundAlt">#E6ECFF</Color>
                     <Color x:Key="CardBackgroundAltDark">#2A2F3A</Color>
                     <Color x:Key="PageBackgroundColorLight">#F3F4F6</Color>
                     <Color x:Key="PageBackgroundColorDark">#111827</Color>
                     <Color x:Key="CardBorderColor">#E5E7EB</Color>
                     <Color x:Key="TextSecondaryLight">#6B7280</Color>
                     <Color x:Key="TextSecondaryDark">#CBD5E1</Color>
                     <Color x:Key="TextSecondary">#6B7280</Color>
                     <Color x:Key="AccentColor">#5B5BD6</Color>
                     <Color x:Key="AccentColorLight">#8B8CF1</Color>

                     <!-- Button Styles -->
                     <Style x:Key="ActionButton"
                            TargetType="Button">
                            <Setter Property="FontSize"
                                    Value="12"/>
                            <Setter Property="Padding"
                                    Value="10,6"/>
                            <Setter Property="CornerRadius"
                                    Value="10"/>
                            <Setter Property="HeightRequest"
                                    Value="36"/>
                            <Setter Property="MinimumWidthRequest"
                                    Value="70"/>
                     </Style>

                     <!-- Icon Button Style - square buttons with larger icons -->
                     <Style x:Key="IconButton"
                            TargetType="Button">
                            <Setter Property="FontSize"
                                    Value="16"/>
                            <Setter Property="Padding"
                                    Value="0"/>
                            <Setter Property="CornerRadius"
                                    Value="10"/>
                            <Setter Property="HeightRequest"
                                    Value="36"/>
                            <Setter Property="WidthRequest"
                                    Value="36"/>
                            <Setter Property="MinimumWidthRequest"
                                    Value="36"/>
                            <Setter Property="BackgroundColor"
                                    Value="#F3F4F6"/>
                            <Setter Property="TextColor"
                                    Value="#374151"/>
                            <Setter Property="BorderColor"
                                    Value="#E5E7EB"/>
                            <Setter Property="BorderWidth"
                                    Value="1"/>
                     </Style>

                     <Style x:Key="StartButton"
                            TargetType="Button"
                            BasedOn="{StaticResource IconButton}">
                            <Setter Property="BackgroundColor"
                                    Value="#ECFDF3"/>
                            <Setter Property="TextColor"
                                    Value="#16A34A"/>
                     </Style>

                     <Style x:Key="PauseButton"
                            TargetType="Button"
                            BasedOn="{StaticResource IconButton}">
                            <Setter Property="BackgroundColor"
                                    Value="#FFF7ED"/>
                            <Setter Property="TextColor"
                                    Value="#F97316"/>
                     </Style>

                     <Style x:Key="StopButton"
                            TargetType="Button"
                            BasedOn="{StaticResource IconButton}">
                            <Setter Property="BackgroundColor"
                                    Value="#FEF2F2"/>
                            <Setter Property="TextColor"
                                    Value="#DC2626"/>
                     </Style>

                     <Style x:Key="RemoveButton"
                            TargetType="Button"
                            BasedOn="{StaticResource IconButton}">
                            <Setter Property="BackgroundColor"
                                    Value="#FEF2F2"/>
                            <Setter Property="TextColor"
                                    Value="#DC2626"/>
                     </Style>

                     <Style x:Key="FolderButton"
                            TargetType="Button"
                            BasedOn="{StaticResource IconButton}">
                            <Setter Property="BackgroundColor"
                                    Value="#EFF6FF"/>
                            <Setter Property="TextColor"
                                    Value="#2563EB"/>
                     </Style>

                     <Style x:Key="HeaderIconButton"
                            TargetType="Button">
                            <Setter Property="HeightRequest"
                                    Value="42"/>
                            <Setter Property="WidthRequest"
                                    Value="42"/>
                            <Setter Property="MinimumWidthRequest"
                                    Value="42"/>
                            <Setter Property="Padding"
                                    Value="0"/>
                            <Setter Property="CornerRadius"
                                    Value="14"/>
                            <Setter Property="FontSize"
                                    Value="20"/>
                            <Setter Property="FontAttributes"
                                    Value="None"/>
                            <Setter Property="BackgroundColor"
                                    Value="{AppThemeBinding Light=#FFFFFF, Dark=#17202D}"/>
                            <Setter Property="TextColor"
                                    Value="{AppThemeBinding Light=#55627A, Dark=#E2E8F0}"/>
                            <Setter Property="BorderColor"
                                    Value="{AppThemeBinding Light=#D6DEEA, Dark=#334155}"/>
                            <Setter Property="BorderWidth"
                                    Value="1"/>
                     </Style>
              </ResourceDictionary>
       </ContentPage.Resources>

       <Grid RowDefinitions="Auto,Auto,*"
             Padding="16,12">

              <!-- Header Section -->
              <Grid Grid.Row="0"
                    ColumnDefinitions="*,Auto,Auto"
                    RowDefinitions="Auto"
                    ColumnSpacing="12">
                     <HorizontalStackLayout Grid.Row="0"
                                            Grid.Column="0"
                                            Spacing="10"
                                            VerticalOptions="Center">
                            <Image Source="app_logo.svg"
                                   WidthRequest="28"
                                   HeightRequest="28"/>
                            <VerticalStackLayout Spacing="2">
                                   <Label Text="{helpers:Translate AppTitle}"
                                          FontSize="24"
                                          FontAttributes="Bold"
                                          TextColor="{StaticResource AccentColor}"
                                          HorizontalOptions="Start"/>
                                   <Label Text="{helpers:Translate AppSubtitle}"
                                          FontSize="12"
                                          TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"
                                          HorizontalOptions="Start"/>
                            </VerticalStackLayout>
                     </HorizontalStackLayout>
                      <Button Grid.Row="0"
                              Grid.Column="1"
                              Text="ⓘ"
                              SemanticProperties.Description="{helpers:Translate AboutTitle}"
                              Style="{StaticResource HeaderIconButton}"
                              Clicked="OnAboutClicked"/>
                      <Button Grid.Row="0"
                              Grid.Column="2"
                              Text="⚙️"
                              SemanticProperties.Description="{helpers:Translate SettingsTitle}"
                              Style="{StaticResource HeaderIconButton}"
                              Command="{Binding OpenSettingsCommand}"/>
              </Grid>

              <!-- Input Section -->
              <VerticalStackLayout Grid.Row="1"
                                   Spacing="8"
                                   Margin="0,16,0,16">
                     <Border Padding="12"
                             Stroke="{StaticResource CardBorderColor}"
                             Background="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                            <Border.StrokeShape>
                                   <RoundRectangle CornerRadius="12"/>
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="*,Auto,Auto"
                                  ColumnSpacing="8">
                                    <Entry Grid.Column="0"
                                           Placeholder="{helpers:Translate AddTorrentPlaceholder}"
                                           SemanticProperties.Description="{helpers:Translate AddTorrentPlaceholder}"
                                          SemanticProperties.Hint="{helpers:Translate MagnetInputHint}"
                                           Text="{Binding MagnetLinkInput}"
                                          ReturnCommand="{Binding AddTorrentCommand}"
                                          FontSize="14"
                                          ClearButtonVisibility="WhileEditing"
                                          Keyboard="Url"
                                          MaxLength="4000"/>

                                    <Button Grid.Column="1"
                                            Text="{helpers:Translate PasteButton}"
                                           SemanticProperties.Description="{helpers:Translate PasteButton}"
                                            Command="{Binding PasteMagnetLinkCommand}"
                                           BackgroundColor="#EEF2FF"
                                           TextColor="#4F46E5"
                                           FontSize="14"
                                           FontAttributes="Bold"
                                           CornerRadius="8"
                                           Padding="12,8"
                                           HeightRequest="44"/>

                                    <Button Grid.Column="2"
                                            Text="{helpers:Translate BrowseButton}"
                                           SemanticProperties.Description="{helpers:Translate SelectTorrentFile}"
                                            Command="{Binding BrowseTorrentFileCommand}"
                                           BackgroundColor="{StaticResource AccentColor}"
                                           TextColor="White"
                                           FontSize="14"
                                           FontAttributes="Bold"
                                           CornerRadius="8"
                                           Padding="16,8"
                                           HeightRequest="44"/>
                            </Grid>
                     </Border>

                     <Label Text="{Binding ErrorMessage}"
                            TextColor="{StaticResource StatusFailed}"
                            IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"
                            FontSize="12"
                            HorizontalOptions="Center"/>

                     <Border Padding="12"
                             Stroke="{StaticResource CardBorderColor}"
                             Background="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                            <Border.StrokeShape>
                                   <RoundRectangle CornerRadius="12"/>
                            </Border.StrokeShape>
                            <VerticalStackLayout Spacing="8">
                                   <Label Text="{helpers:Translate Bandwidth}"
                                          FontSize="13"
                                          FontAttributes="Bold"/>
                                   <Grid ColumnDefinitions="*,*"
                                         ColumnSpacing="12">
                                          <VerticalStackLayout Grid.Column="0"
                                                               Spacing="4">
                                                 <Label Text="{helpers:Translate DownloadKBs}"
                                                        FontSize="10"
                                                        TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                 <controls:LineChartView Values="{Binding GlobalDownloadHistory}"
                                                                         LineColor="#42A5F5"
                                                                         FillColor="#2642A5F5"
                                                                         HeightRequest="48"/>
                                          </VerticalStackLayout>
                                          <VerticalStackLayout Grid.Column="1"
                                                               Spacing="4">
                                                 <Label Text="{helpers:Translate UploadKBs}"
                                                        FontSize="10"
                                                        TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                 <controls:LineChartView Values="{Binding GlobalUploadHistory}"
                                                                         LineColor="#26A69A"
                                                                         FillColor="#2626A69A"
                                                                         HeightRequest="48"/>
                                          </VerticalStackLayout>
                                   </Grid>
                            </VerticalStackLayout>
                     </Border>
              </VerticalStackLayout>

              <!-- Downloads Section -->
              <Grid Grid.Row="2"
                    RowDefinitions="Auto,Auto,*">
                     <Grid Grid.Row="0"
                           ColumnDefinitions="*,Auto,Auto,Auto"
                           ColumnSpacing="12"
                           Margin="0,0,0,12">
                            <Label Grid.Column="0"
                                   Text="{helpers:Translate DownloadsTitle}"
                                   FontSize="18"
                                   FontAttributes="Bold"/>
                            <HorizontalStackLayout Grid.Column="1"
                                                   Spacing="8"
                                                   VerticalOptions="Center">
                                     <Button Text="{helpers:Translate StartAllButton}"
                                             SemanticProperties.Description="{helpers:Translate StartAllButton}"
                                             Style="{StaticResource ActionButton}"
                                            BackgroundColor="#ECFDF3"
                                            TextColor="#16A34A"
                                            Command="{Binding StartAllTorrentsCommand}"
                                            IsEnabled="{Binding CanStartAllTorrents}"/>
                                     <Button Text="{helpers:Translate StopAllButton}"
                                             SemanticProperties.Description="{helpers:Translate StopAllButton}"
                                             Style="{StaticResource ActionButton}"
                                            BackgroundColor="#FEF2F2"
                                            TextColor="#DC2626"
                                            Command="{Binding StopAllTorrentsCommand}"
                                            IsEnabled="{Binding CanStopAllTorrents}"/>
                             </HorizontalStackLayout>
                             <HorizontalStackLayout Grid.Column="2"
                                                   Spacing="6"
                                                   VerticalOptions="Center">
                                    <CheckBox IsChecked="{Binding SortByStatus}"
                                             SemanticProperties.Description="{helpers:Translate DownloadingOnTop}"
                                             SemanticProperties.Hint="{helpers:Translate DownloadingOnTopHint}"/>
                                   <Label Text="{helpers:Translate DownloadingOnTop}"
                                          FontSize="12"
                                          TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"
                                          VerticalOptions="Center"/>
                            </HorizontalStackLayout>
                              <Button Grid.Column="3"
                                     Text="{helpers:Translate Details}"
                                     SemanticProperties.Description="{helpers:Translate Details}"
                                     Style="{StaticResource ActionButton}"
                                    BackgroundColor="#EEF2FF"
                                    TextColor="#4F46E5"
                                    Command="{Binding ToggleSelectedTorrentDetailsCommand}"
                                    IsEnabled="{Binding SelectedTorrent, Converter={StaticResource ObjectNotNullConverter}}"/>
                     </Grid>

                     <Border Grid.Row="1"
                             Padding="12"
                             Stroke="{StaticResource CardBorderColor}"
                             Background="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}"
                             IsVisible="{Binding CanShowSelectedTorrentDetails}">
                            <Border.StrokeShape>
                                   <RoundRectangle CornerRadius="12"/>
                            </Border.StrokeShape>
                            <Grid RowDefinitions="Auto,Auto,Auto"
                                  ColumnDefinitions="*,*"
                                  ColumnSpacing="12"
                                  RowSpacing="8">
                                   <Label Grid.Row="0"
                                          Grid.ColumnSpan="2"
                                          Text="{helpers:Translate SelectedTorrentLimits}"
                                          FontSize="13"
                                          FontAttributes="Bold"/>

                                   <VerticalStackLayout Grid.Row="1"
                                                        Grid.Column="0"
                                                        Spacing="4">
                                          <Label Text="{helpers:Translate DownloadKBs}"
                                                 FontSize="10"
                                                 TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                           <Entry Text="{Binding SelectedTorrent.DownloadLimitKbps}"
                                                 SemanticProperties.Description="{helpers:Translate DownloadKBs}"
                                                 SemanticProperties.Hint="{helpers:Translate GlobalHint}"
                                                 Keyboard="Numeric"
                                                 Placeholder="{helpers:Translate GlobalHint}"/>
                                   </VerticalStackLayout>

                                   <VerticalStackLayout Grid.Row="1"
                                                        Grid.Column="1"
                                                        Spacing="4">
                                          <Label Text="{helpers:Translate UploadKBs}"
                                                 FontSize="10"
                                                 TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                           <Entry Text="{Binding SelectedTorrent.UploadLimitKbps}"
                                                 SemanticProperties.Description="{helpers:Translate UploadKBs}"
                                                 SemanticProperties.Hint="{helpers:Translate GlobalHint}"
                                                 Keyboard="Numeric"
                                                 Placeholder="{helpers:Translate GlobalHint}"/>
                                   </VerticalStackLayout>

                                   <VerticalStackLayout Grid.Row="2"
                                                        Grid.Column="0"
                                                        Spacing="4">
                                          <Label Text="{helpers:Translate MaxSeedRatio}"
                                                 FontSize="10"
                                                 TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                           <Entry Text="{Binding SelectedTorrent.MaxSeedRatio}"
                                                 SemanticProperties.Description="{helpers:Translate MaxSeedRatio}"
                                                 SemanticProperties.Hint="{helpers:Translate UnlimitedHint}"
                                                 Keyboard="Numeric"
                                                 Placeholder="{helpers:Translate GlobalHint}"/>
                                   </VerticalStackLayout>

                                   <VerticalStackLayout Grid.Row="2"
                                                        Grid.Column="1"
                                                        Spacing="4">
                                          <Label Text="{helpers:Translate MaxSeedMinutes}"
                                                 FontSize="10"
                                                 TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                           <Entry Text="{Binding SelectedTorrent.MaxSeedMinutes}"
                                                 SemanticProperties.Description="{helpers:Translate MaxSeedMinutes}"
                                                 SemanticProperties.Hint="{helpers:Translate UnlimitedHint}"
                                                 Keyboard="Numeric"
                                                 Placeholder="{helpers:Translate GlobalHint}"/>
                                   </VerticalStackLayout>
                            </Grid>
                     </Border>

                     <!-- Empty State -->
                     <VerticalStackLayout Grid.Row="2"
                                          IsVisible="{Binding IsEmpty}"
                                          VerticalOptions="Center"
                                          HorizontalOptions="Center"
                                          Spacing="16">
                            <Label Text="{helpers:Translate NoDownloadsYet}"
                                   FontSize="18"
                                   FontAttributes="Bold"
                                   HorizontalOptions="Center"/>
                            <Label Text="{helpers:Translate AddMagnetHint}"
                                   FontSize="14"
                                   TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"
                                   HorizontalOptions="Center"/>
                     </VerticalStackLayout>

                     <!-- Downloads List -->
                      <CollectionView Grid.Row="2"
                                      ItemsSource="{Binding DisplayTorrents}"
                                      SemanticProperties.Description="{helpers:Translate DownloadsTitle}"
                                      SelectionMode="Single"
                                     SelectedItem="{Binding SelectedTorrent}"
                                     IsVisible="{Binding IsEmpty, Converter={StaticResource InverseBoolConverter}}">
                            <CollectionView.ItemTemplate>
                                   <DataTemplate x:DataType="models:TorrentItem">
                                          <Border Margin="0,0,0,12"
                                                  Padding="12"
                                                  Stroke="{StaticResource CardBorderColor}"
                                                  Background="{AppThemeBinding Light={StaticResource CardBackground}, Dark={StaticResource CardBackgroundDark}}">
                                                 <Border.StrokeShape>
                                                        <RoundRectangle CornerRadius="12"/>
                                                 </Border.StrokeShape>
                                                 <Border.Triggers>
                                                        <DataTrigger TargetType="Border"
                                                                     Binding="{Binding DisplayIndex, Converter={StaticResource OddIndexConverter}}"
                                                                     Value="True">
                                                               <Setter Property="Background"
                                                                       Value="{AppThemeBinding Light={StaticResource CardBackgroundAlt}, Dark={StaticResource CardBackgroundAltDark}}"/>
                                                        </DataTrigger>
                                                 </Border.Triggers>
                                                 <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto"
                                                       RowSpacing="8">

                                                        <!-- Row 0: Name and Status -->
                                                        <Grid Grid.Row="0"
                                                              ColumnDefinitions="*,Auto,Auto"
                                                              ColumnSpacing="8">
                                                               <VerticalStackLayout Grid.Column="0"
                                                                                    Spacing="2">
                                                                      <Label Text="{Binding Name}"
                                                                             FontSize="15"
                                                                             FontAttributes="Bold"
                                                                             LineBreakMode="TailTruncation"
                                                                             MaxLines="2"/>
                                                                      <Label Text="{Binding ErrorMessage}"
                                                                             FontSize="11"
                                                                             TextColor="{StaticResource StatusFailed}"
                                                                             IsVisible="{Binding ErrorMessage, Converter={StaticResource StringNotEmptyConverter}}"
                                                                             LineBreakMode="TailTruncation"
                                                                             MaxLines="2"/>
                                                                      <Label Text="{Binding StatusHint}"
                                                                             FontSize="11"
                                                                             TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"
                                                                             IsVisible="{Binding StatusHint, Converter={StaticResource StringNotEmptyConverter}}"
                                                                             LineBreakMode="TailTruncation"
                                                                             MaxLines="2"/>
                                                               </VerticalStackLayout>
                                                               <Button Grid.Column="1"
                                                                       Text="📂"
                                                                       SemanticProperties.Description="{helpers:Translate ShowInFolder}"
                                                                       Style="{StaticResource FolderButton}"
                                                                       Command="{Binding ShowInFolderCommand}"
                                                                       CommandParameter="{Binding .}"
                                                                       IsEnabled="{Binding CanOpenDownloadedFile}"
                                                                       Opacity="0.5">
                                                                      <Button.Triggers>
                                                                             <DataTrigger TargetType="Button"
                                                                                          Binding="{Binding CanOpenDownloadedFile}"
                                                                                          Value="True">
                                                                                    <Setter Property="Opacity"
                                                                                            Value="1"/>
                                                                             </DataTrigger>
                                                                      </Button.Triggers>
                                                               </Button>
                                                               <Border Grid.Column="2"
                                                                       Padding="8,4"
                                                                       Stroke="Transparent"
                                                                       Background="{Binding Status, Converter={StaticResource StatusColorConverter}}">
                                                                      <Border.StrokeShape>
                                                                             <RoundRectangle CornerRadius="12"/>
                                                                      </Border.StrokeShape>
                                                                      <Label Text="{Binding StatusText}"
                                                                             FontSize="11"
                                                                             TextColor="White"
                                                                             FontAttributes="Bold"/>
                                                               </Border>
                                                        </Grid>

                                                        <!-- Row 1: Progress Bar -->
                                                        <Grid Grid.Row="1"
                                                              ColumnDefinitions="*,Auto"
                                                              ColumnSpacing="8">
                                                                 <ProgressBar Grid.Column="0"
                                                                             Progress="{Binding Progress, Converter={StaticResource ProgressConverter}}"
                                                                             SemanticProperties.Description="{helpers:Translate DownloadProgress}"
                                                                             ProgressColor="{StaticResource AccentColor}"
                                                                            HeightRequest="6"/>
                                                               <Label Grid.Column="1"
                                                                      Text="{Binding Progress, StringFormat='{0:F0}%'}"
                                                                      FontSize="10"
                                                                      TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"
                                                                      VerticalOptions="Center"/>
                                                        </Grid>

                                                        <!-- Row 2: Stats -->
                                                        <Grid Grid.Row="2"
                                                              ColumnDefinitions="*,*,*,*,*,*"
                                                              ColumnSpacing="8">
                                                               <VerticalStackLayout Grid.Column="0"
                                                                                    Spacing="2">
                                                                      <Label Text="{helpers:Translate Size}"
                                                                             FontSize="10"
                                                                             TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                      <Label Text="{Binding FormattedTotalSize}"
                                                                             FontSize="12"
                                                                             FontAttributes="Bold"/>
                                                               </VerticalStackLayout>
                                                               <VerticalStackLayout Grid.Column="1"
                                                                                    Spacing="2">
                                                                      <Label Text="{helpers:Translate Down}"
                                                                             FontSize="10"
                                                                             TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                      <Label Text="{Binding FormattedDownloadSpeed}"
                                                                             FontSize="12"
                                                                             FontAttributes="Bold"/>
                                                               </VerticalStackLayout>
                                                               <VerticalStackLayout Grid.Column="2"
                                                                                    Spacing="2">
                                                                      <Label Text="{helpers:Translate Up}"
                                                                             FontSize="10"
                                                                             TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                      <Label Text="{Binding FormattedUploadSpeed}"
                                                                             FontSize="12"
                                                                             FontAttributes="Bold"/>
                                                               </VerticalStackLayout>
                                                               <VerticalStackLayout Grid.Column="3"
                                                                                    Spacing="2">
                                                                      <Label Text="{helpers:Translate Seeds}"
                                                                             FontSize="10"
                                                                             TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                      <Label Text="{Binding Seeders}"
                                                                             FontSize="12"
                                                                             FontAttributes="Bold"/>
                                                               </VerticalStackLayout>
                                                               <VerticalStackLayout Grid.Column="4"
                                                                                    Spacing="2">
                                                                      <Label Text="{helpers:Translate Peers}"
                                                                             FontSize="10"
                                                                             TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                      <Label Text="{Binding Leechers}"
                                                                             FontSize="12"
                                                                             FontAttributes="Bold"/>
                                                               </VerticalStackLayout>
                                                               <VerticalStackLayout Grid.Column="5"
                                                                                    Spacing="2">
                                                                      <Label Text="{helpers:Translate ETA}"
                                                                             FontSize="10"
                                                                             TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                      <Label Text="{Binding FormattedEstimatedTime}"
                                                                             FontSize="12"
                                                                             FontAttributes="Bold"/>
                                                               </VerticalStackLayout>
                                                        </Grid>

                                                        <!-- Row 3: Health & Availability -->
                                                        <Grid Grid.Row="3"
                                                              ColumnDefinitions="*,*"
                                                              ColumnSpacing="12">
                                                               <VerticalStackLayout Grid.Column="0"
                                                                                    Spacing="4">
                                                                      <Grid ColumnDefinitions="*,Auto">
                                                                             <Label Grid.Column="0"
                                                                                    Text="{helpers:Translate Health}"
                                                                                    FontSize="10"
                                                                                    TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                             <Label Grid.Column="1"
                                                                                    Text="{Binding HealthScore}"
                                                                                    FontSize="10"
                                                                                    FontAttributes="Bold"/>
                                                                      </Grid>
                                                                      <ProgressBar Progress="{Binding HealthScore, Converter={StaticResource ProgressConverter}}"
                                                                                   ProgressColor="#FF7043"
                                                                                   HeightRequest="6"/>
                                                               </VerticalStackLayout>
                                                               <VerticalStackLayout Grid.Column="1"
                                                                                    Spacing="4">
                                                                      <Grid ColumnDefinitions="*,Auto">
                                                                             <Label Grid.Column="0"
                                                                                    Text="{helpers:Translate Availability}"
                                                                                    FontSize="10"
                                                                                    TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                             <Label Grid.Column="1"
                                                                                    Text="{Binding AvailabilityLabel}"
                                                                                    FontSize="10"
                                                                                    FontAttributes="Bold"/>
                                                                      </Grid>
                                                                      <ProgressBar Progress="{Binding AvailabilityPercent, Converter={StaticResource ProgressConverter}}"
                                                                                   ProgressColor="#7E57C2"
                                                                                   HeightRequest="6"/>
                                                               </VerticalStackLayout>
                                                        </Grid>

                                                        <!-- Row 4: Bandwidth Charts -->
                                                        <Grid Grid.Row="4"
                                                              ColumnDefinitions="*,*"
                                                              ColumnSpacing="12">
                                                               <VerticalStackLayout Grid.Column="0"
                                                                                    Spacing="4">
                                                                      <Label Text="{helpers:Translate DownloadKBs}"
                                                                             FontSize="10"
                                                                             TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                      <controls:LineChartView Values="{Binding DownloadSpeedHistory}"
                                                                                              LineColor="#42A5F5"
                                                                                              FillColor="#2642A5F5"
                                                                                              HeightRequest="40"/>
                                                               </VerticalStackLayout>
                                                               <VerticalStackLayout Grid.Column="1"
                                                                                    Spacing="4">
                                                                      <Label Text="{helpers:Translate UploadKBs}"
                                                                             FontSize="10"
                                                                             TextColor="{AppThemeBinding Light={StaticResource TextSecondaryLight}, Dark={StaticResource TextSecondaryDark}}"/>
                                                                      <controls:LineChartView Values="{Binding UploadSpeedHistory}"
                                                                                              LineColor="#26A69A"
                                                                                              FillColor="#2626A69A"
                                                                                              HeightRequest="40"/>
                                                               </VerticalStackLayout>
                                                        </Grid>

                                                        <!-- Row 5: Action Buttons -->
                                                        <HorizontalStackLayout Grid.Row="5"
                                                                               Spacing="8"
                                                                               HorizontalOptions="End"
                                                                               Margin="0,8,0,0">
                                                                <Button Text="▶️"
                                                                        SemanticProperties.Description="{helpers:Translate StartButton}"
                                                                        Style="{StaticResource StartButton}"
                                                                       Command="{Binding StartSpecificTorrentCommand}"
                                                                       CommandParameter="{Binding .}"
                                                                       IsVisible="{Binding CanStart}"/>
                                                                <Button Text="⏸️"
                                                                        SemanticProperties.Description="{helpers:Translate PauseButton}"
                                                                        Style="{StaticResource PauseButton}"
                                                                       Command="{Binding PauseSpecificTorrentCommand}"
                                                                       CommandParameter="{Binding .}"
                                                                       IsVisible="{Binding CanPause}"/>
                                                                <Button Text="⏹️"
                                                                         SemanticProperties.Description="{helpers:Translate StopButton}"
                                                                        Style="{StaticResource StopButton}"
                                                                       Command="{Binding StopSpecificTorrentCommand}"
                                                                       CommandParameter="{Binding .}"
                                                                       IsVisible="{Binding CanStop}"/>
                                                                <Button Text="🗑️"
                                                                        SemanticProperties.Description="{helpers:Translate RemoveButton}"
                                                                        Style="{StaticResource RemoveButton}"
                                                                       Command="{Binding RemoveSpecificTorrentCommand}"
                                                                       CommandParameter="{Binding .}"/>
                                                        </HorizontalStackLayout>
                                                 </Grid>
                                          </Border>
                                   </DataTemplate>
                            </CollectionView.ItemTemplate>
                     </CollectionView>
              </Grid>

              <!-- Loading Indicator -->
               <ActivityIndicator Grid.RowSpan="3"
                                  IsRunning="{Binding IsBusy}"
                                  IsVisible="{Binding IsBusy}"
                                  SemanticProperties.Description="{helpers:Translate LoadingTorrentData}"
                                  Color="{StaticResource AccentColor}"
                                 VerticalOptions="Center"
                                 HorizontalOptions="Center"/>
       </Grid>

</ContentPage>
